# LW Vision 完美化路线图

> 制定日期：2026-08-29 ｜ 依据：代码现状盘点 + 竞品调研（见 `docs/竞品调研-AI视觉软件-2026-08.md`）+ 产品定位确认
>
> **产品定位**：通用工业视觉平台 —— 不止五金紧固件，还要覆盖手机零部件、塑料件、精密件，并适配视觉贴标、木业检测等设备类型；AI 检测能力是标配，要"像人眼人脑一样看出缺陷点"。

---

## 阶段0：工程地基 ✅（2026-08-29 完成）

| 事项 | 结果 |
|------|------|
| git 版本管理 | 已建仓库并提交基线（含 .gitignore） |
| 清理 macOS 垃圾文件 | 348 个 `._*` 文件已删除 |
| 路径可移植化 | CMakePresets/CMakeLists 中 D:/F:/E: 硬编码全部移除；OpenCV 配置改为相对自身路径 |
| 一键编译 | `build.bat` 自动探测 Qt 位置，双击即可编译 |
| 本机编译验证 | Debug + Release 全量编译通过，offscreen 冒烟测试通过 |
| 命名统一 | 可执行文件统一为 `LWVision.exe`（产品名：LW Vision） |
| 文档统一 | `Windows编译指南.md` 为唯一权威编译文档，SETUP.md 改为导航页 |
| 竞品调研 | 军哥MIP / DLCV.AI / 广州视觉芯，结论沉淀在 `docs/竞品调研-AI视觉软件-2026-08.md` |

> ⚠️ 待办：在 GitHub/Gitee 建一个**私有远端仓库**并 push（移动硬盘不能是唯一副本），需要时告知仓库地址即可。

---

## 阶段1：运行时架构正本清源 ✅（2026-08-29 完成）

**目标**：软件从"演示壳"变成"正确的工业软件架构"。

1. **执行引擎多线程化 + 数据流打通** ✅
   - 执行移入 QtConcurrent 工作线程；`stopRunning` 原子中止（实测停止响应 0ms）
   - Flow 工具列表 QMutex 保护，UI 编辑与工作线程快照迭代互不干扰
   - 工具结果以"工具名.键"写入全局命名空间 + 按工具归档；任意字符串属性支持 `$(工具名.键)` 引用解析（RAII）
   - 回归测试 tests/test_engine 9 项全过
2. **虚拟相机驱动** ✅
   - `src/hal/VirtualCamera`：图像文件夹回放 + 内置合成图案（转盘工件/螺纹牙型/周期NG），无硬件全流程开发演示
   - 无 DVP2 SDK 的电脑自动启用；相机菜单可切换
3. **PLC 模拟器 + Modbus 真通讯** ✅
   - 自研 Modbus TCP 主站（FC1/2/3/4/5/6/10/15，连接池）+ 从站模拟器（四寄存器区+自动行为）
   - 设置菜单[PLC模拟器]：启停/寄存器实时表/动作记录；MB读/写工具重写为自带连接参数
   - 自动化测试 tests/test_modbus 12 项全过
4. **补齐"待实现"** ✅
   - 系统设置（日志级别/图像保存目录）、项目设置、全局变量管理、用户登录（密码持久化 config/users.json，角色启用/禁用编辑功能）
5. **项目保存/加载闭环** ✅
   - .vipj（JSON）：项目信息+全部流程+工具属性+全局变量；工具类型缺失容错；主窗口接线生效
6. **测试资产** ✅
   - tools/testdata_gen 确定性合成螺纹样本库（OK 24张 + 缺牙/毛刺/烂牙/斜牙各6张），generate_testdata 构建目标

## 阶段2：通用工具打磨到"能用好用"（对标 CKVision 的工具质量）

1. **OpenCV 重建加入 contrib** ✅（2026-08-29 完成）
   - 重建捆绑 OpenCV 5.0.0 加入 ximgproc(EdgeDrawing/亚像素边线圆) + calib(标定) 模块
   - 验证：tests/test_edgedrawing 通过——合成厚环找圆圆心误差 0.00px、半径误差 0.07px（内外沿均值）
   - 一键重建脚本：tools/rebuild_opencv.bat
2. **算法质量** ✅（2026-08-29 完成核心部分）
   - 新建 `src/engine/SubpixEdge` 亚像素测量库: 双线性采样、卡尺式亚像素边缘(梯度峰值+抛物线内插)、鲁棒圆/直线拟合(代数拟合+中位数残差剔外点)
   - 检测圆形/检测直线 重写为亚像素版(修复PCA特征向量符号bug), 输出rms; 新增 快速找圆 工具(EdgeDrawing整图搜索, 无需先验位置)
   - 结果叠加层: ImageViewWidget 缩放/平移查看器(架构参考OpenIVS) + 工具overlays图形描述 + 主界面实时绘制
   - 回归: tests/test_tools_regression 174项检查零硬失败, 48图找圆43/48, 亚像素半径误差≤0.87px
   - 回退定位 ✅(2026-08-30): 毛刺/缺牙破坏外缘整圆弧时 EdgeDrawing 无候选, 新增 Hough 圆检测(HOUGH_GRADIENT_ALT)整图投票 + 卡尺亚像素细化回退, 48图找圆48/48, 亚像素48/48, 硬失败0
3. **交互与图像功能** ✅（2026-08-29 完成）
   - 卡尺测量亚像素化(旋转卡尺+梯度峰值内插, 输出边位置/强度)
   - 保存带标注图: 文件菜单手动保存 + 系统设置可开"NG图像自动保存(含标注)"
   - 属性对话框四页签化(对齐CKVision): 基本设置(名/注释/启用)/参数设置/数据判定/试执行(用最近帧预览检测结果叠加)
2. **数据流打通**：找圆→位置补正→后续工具 ROI 自动跟随（这是 CKVision 流程的灵魂，目前节点连线并不真正传数据）
3. **结果可视化**：每个工具在图像上叠加 ROI/检测图形/OK/NG 标注，颜色可配置
4. **图像查看器升级**：缩放/平移/网格/标尺/像素信息，多画面布局 1~8 分屏
5. **逐工具验收表**：40+ 工具每个都用标准图片库回归验证，结果记录在案

## 阶段3：螺纹检测专项（拳头功能）—— 核心完成 ✅（2026-08-29）

**ThreadInspection 螺纹检测工具已落地**（`plugins/tools/ThreadInspection`）：
- 管线：链式定位(快速找圆) → warpPolar 极坐标展开 → 螺纹环带角度投影 → 平滑+峰值合并 → 缺陷分析
- 输出：牙数/牙距(角度+像素)/缺牙数/烂牙数/毛刺数/斜牙角/牙外径；结果叠加层画牙型刻线(绿)/缺牙(红)/烂牙(橙)/毛刺(红点)
- 回归（tests/test_thread, 48图）：OK图牙数 24/24 全对、缺牙检出 6/6、牙距误差 0.00°
- 调试中修复：warpPolar 轴向映射(本构建 x=半径/y=角度)、角度平滑窗口、峰合并策略
- 已入筛选机模板（此前留坑位置）
**待完善（后续迭代）**：烂牙/斜牙判定阈值参数化验证（合成样本已可跑通, 需真实样本调优）；找圆失败的卡尺回退定位；MVTec AD/M2 真实图库验证。
配套专用教导界面（阶段3.5）与真实样本调优随后推进。

## 阶段4：AI 检测能力 —— 推理管线完成 ✅（2026-08-30）

**已落地**：
- ONNX Runtime 1.18.1 集成（thirdparty/onnxruntime, MinGW+SAL兼容层, 可选依赖: 无DLL时AI节点自动禁用其余功能不受影响）
- `src/ai/InferEngine`: 模型加载/letterbox预处理/推理/YOLOv8解码+NMS/模型全局缓存(PIMPL)
- **AIDetection 工具节点**: 流程编辑器直接拖入AI检测, 检出即NG可配, 检测框+类别+置信度叠加层
- yolov8n.onnx 预训练模型已就位(testdata/, 12.8MB); Python训练侧环境(ultralytics)已装好
- 真实样本图推理验证: **CPU 52ms**, 框坐标合理, 模型缓存复用 ✓ (tests/test_ai)
**已完成补充**（2026-08-30）：
- 标注工具 ✅（标注菜单, YOLO格式, 导出训练集+data.yaml）
- **第一个自训缺陷分类模型 ✅**：175张真实图6类(良品/开裂/发黑/头疤/电镀不良/未点胶), yolov8n-cls 30轮, ONNX 5.5MB
- C++推理与ultralytics完全对齐(验证集63.8%逐类一致, CPU 50ms/张)
- 开发主阵地迁移至 D:\LWVision（内置NTFS, 根除移动硬盘写入丢失问题）
**提升方向**：准确率63.8%→90%+（更多数据/更多轮次/GPU/类别均衡）；PaDiM无监督节点；3060 CUDA EP

**落地部署完成 ✅（2026-08-30）**：
- tools/deploy.bat 一键打包: 发布包release_pkg/LWVision (213MB, 自包含: exe+Qt+OpenCV+ONNX Runtime+模型+测试图)
- 包内启动验证通过(脱机可运行); models/缺陷分类.onnx(79.7%准确率)+通用yolov8n预训练模型随包
- docs/工控机部署指南.md: 相机接入规范/多相机现场经验/AI使用/产线运行建议/常见问题
- 第二个自训模型: 合并长+短图库263张7类, 100轮+早停, Top-1 79.7%(良品误杀15%可调阈值)

1. **ONNX Runtime 推理节点**：C++ 端封装（CPU 为底线，显卡可选加速），支持 YOLOv8 检测/分类/分割三类模型，输入输出对齐 OpenCV Mat；接口形状与 JSON 结果语义**对齐 OpenIVS 标准**（`category_id`/`box`/`angle`/`region`/`mask_rle` + 耗时信息，见 `docs/OpenIVS专项评估.md`）
2. **标注-训练闭环**：软件内简化标注（矩形/多边形）→ 一键调用训练（Python 脚本，YOLOv8/分类迁移学习）→ 导出 ONNX → 回到软件推理；学 DLCV"标训测部一体"，避免视觉芯"双软件断链"的坑
3. **无监督异常检测节点**：纯 OK 图即可用；anomalib 训练 PaDiM（优先，轻量）导出 ONNX，C++ 推理端复刻预处理/后处理（已知的分数对齐坑，见 `docs/开源引入调研-2026-08.md`）
4. **硬件定位（已确认）**：部署/训练机显卡上限按 **RTX 3060（桌面版 12GB）** 档设计
   - 推理：CPU EP 为底线（无卡可跑），检测到显卡自动走 CUDA EP，进阶可切 TensorRT EP
   - 估算余量：YOLOv8s@640 约 50~150 FPS，覆盖 8 相机 × 5 张/秒（300颗/分钟满配）仍有富余；阶段4 立项后用真实模型跑基准核实
   - 训练：100 张图迁移学习微调为分钟级，支撑"30分钟从标图到上线"目标
   - 部署注意：CUDA/cuDNN 运行库随安装包管理；产线机关闭显卡驱动自动更新；GPU 调用设超时保护
5. **验收**：零基础用户 30 分钟内完成"标 100 张图 → 训练 → 上线检测"全流程

## 阶段5：通讯与整机闭环（等真机到位）

信捷 XD3/XD5 Modbus TCP/RTU 真机联调、拍照触发→检测→吹气 IO 时序、伺服转速联动、1~8 相机即插即用、异常恢复与看门狗。

**PLC 联动专项（2026-08-30，按 8工位转盘筛选机 契约落地）**：
- **Modbus RTU 主站/从站自研完成**：串口 RS232/485 全功能码（FC1/2/3/4/5/6/15/16），CRC16 校验、连接池、阻塞式 API 适配流程工作线程（`src/hal/ModbusRtuMaster/Slave`）
- **MB读/写工具双通道**：Modbus TCP + Modbus RTU(串口) 可选，COM 口/波特率/数据位/停止位/校验 参数化
- **新增 `PLC联动` 工具**：13 种操作直映射筛选机契约——启动/停止/产量清零(D6)、读产量统计(HD170/172/174/176)、读相机位置(HD200~214)、相机手动拍照(M121~135)、手动OK/NG剔除(M116/117)、速度设置(HD0/HD8)、相机使能(HM1~8)、读剔除位置(HD400/402)，TCP/RTU 双通道
- **PLC 模拟器双通道化**：TCP/RTU 从站可选 + 转盘筛选机契约地址预设表（含注释），无硬件即可联调整套联动流程
- **ModbusIoDriver（IIODriver 实现）**：经 PLC Modbus 通道替代独立 IO 卡（N1616），coilOut 线圈/结果寄存器双模式、同步脉冲输出（吹气）、读输入线圈/离散输入；无硬件即可驱动筛选机结果链路
- **自动化回归**：tests/test_modbus 扩展 RTU 联测（QLocalSocket 虚拟串口线）——TCP 6 项 + RTU 7 项 + IO 驱动联测全过，含契约高地址读写、异常响应、FC15 多线圈、写入信号、IO 写/读/脉冲
- **N1616 IO 卡替代调研**：子代理选型报告 → `docs/调研/N1616替代选型/N1616替代硬件选型调研报告.md`
  - 用户点名的 ADAM-4055（8DO）/4056SO（12DO）均不满足 16DO；真正 16DO 只有 ICP DAS I-8056/8057/ET-7042
  - **推荐：ICP DAS ET-7042**（16 路隔离开漏 Sink、3750VDC、Modbus TCP 无板卡驱动、与 N1616 电气/接线一致），RS-485 版备选 I-8057
  - 免硬件路线（上位机 Modbus 直写 M/D）**技术可行**（XD5-48T-E 支持 Modbus TCP），但必须加"心跳看门狗判 NG"；作为低成本备选/冗余通道

**PLC 驱动层补全（2026-08-31）**：
- **新增 `ModbusPLCDriver`（IPLCDriver 完整实现）**：信捷 XD Modbus 驱动——Modbus TCP/RTU 双通道 + 全部核心读写（保持/输入寄存器、线圈/离散输入）+ 便捷方法 + 业务级接口（blowOK/blowNG 吹气、readServoPosition、setDiskSpeed、isMaterialReady）+ 契约快捷方法（sendCommand/产量统计/良率/UPH/手动拍照/手动OK/NG/伺服启停），接线 `HardwareManager::connectPLC`
- **补齐 `IPLCDriver.cpp` 便捷方法默认实现**（readRegister/writeBool/readInt32/readFloat 等，由核心方法组合，派生类免重复实现）
- **ModbusIoDriver 接线 `HardwareManager::connectIO`**：IO 驱动经 PLC Modbus 通道替代独立 IO 卡
- **修复 TCP 从站 FC15 写多线圈位偏移 off-by-one**（数据应自 pdu[6] 读，误用 pdu[5]=byteCount；单线圈写 false 会被写成 true——旧测试数据恰被 byteCount 低位掩盖未暴露）
- **联测扩展**：tests/test_modbus 新增 TCP测试8（ModbusPLCDriver 业务契约全链路：命令/速度/产量/伺服/吹气/手动拍照），全 12 项 ctest 通过

**三线排查（2026-08-31，子代理只读盘点，报告在 `docs/排查/`）**：功能缺口、UI 交互、代码质量三份报告——最高优先级：位置补正数据流打通（ROI 跟随）、UI 死功能信号接线、标定真实化（详见报告与"明日完善清单"）

**引擎线程生命周期止血（2026-08-31，代码质量报告"先止血"批次）**：
- **H-14 ToolStatus 注册 QMetaType**：工作线程 emit 的 toolStatusChanged 跨线程 queued 信号此前被 Qt 静默丢弃（节点状态灯永不更新），已 `Q_DECLARE_METATYPE` + `qRegisterMetaType`
- **H-3 执行互斥**：`FlowEngine` 新增 `m_execMutex`，单次执行与连续运行不可并发 `doExecute` 同一 Flow（此前 runLoop 与 executeOnceAsync 可并发改工具属性/结果）
- **H-2/H-11 关窗/加载安全**：新增 `FlowEngine::shutdownAndWait()`（置中止→等待全部工作线程结束）；`MainWindow::closeEvent` 先停引擎再释放相机/Modbus 连接池，并补未保存提示；`ProjectManager::newProject/loadProject` 重建前先停引擎
- **H-1 工具所有权**：`Flow` 拥有工具，析构/clear/removeTool 时 delete（此前从不删除→每次建/载项目泄漏整条流程工具）；H-6 所有权统一归引擎（MainWindow 与 ProjectManager 一致，防关窗双释放）
- **测试扩展**：test_engine 新增测试4（shutdownAndWait 等待慢工具执行完 156ms）/ 测试5（Flow 回收工具不崩），全 12 项 ctest 通过

**UI 信号断链接线（2026-08-31，UI排查 P0 批次）**：
- **P0-1 流程编辑器右键菜单 7 项全接线**：MainWindow 新增 onToolToggleActive/onToolDelete/onToolMoveUp/onToolMoveDown/onToolRename/onToolCopy/onToolPaste，连 toolRename/toolMoveUp/toolMoveDown/toolToggleActive/toolCopy/toolPaste/toolDelete（此前仅"属性"一项有效）；toolCopy 写入 FlowEditor 剪贴板，toolPaste 按类型重建工具并自动命名
- **P0-2 双击节点编辑属性**：连 FlowEditor::toolDoubleClicked → onEditToolProperties
- **P0-3 Delete 键删除节点**：FlowEditor::setupView 连 FlowView::deleteRequested → onSceneDeleteRequested（此前永不触发）
- **P0-4 缩放功能修复**：4 个缩放槽改作用于实际控件 m_multiView->viewAt(0)（此前 m_displayArea 从未 new，100% 失效）；View 菜单补"放大(Ctrl++)/缩小(Ctrl+-)/适应窗口/1:1原大"入口
- **P0-7 状态栏用户/连接标签显示**：此前创建了从未加入状态栏，已 addPermanentWidget
- **P0-5 高频快捷键**：全应用此前无任何快捷键——文件菜单补 Ctrl+N/O/S/Shift+S，操作菜单补 F5 单次执行/F6 连续运行/F7 停止，View 菜单已有 Ctrl++/Ctrl+-
- **P1-8 连续运行不重置缩放/平移**：ImageViewWidget::setImage 改为仅在图像尺寸变化时自动适配（此前每帧 fitToWidget，运行中无法放大细看）；双击/窗口缩放仍保持适配

**位置补正数据流打通（2026-09-01，功能缺口 P0-1：ROI 自动跟随）**：
- **引擎层 `ITool::applyCorrection()`**：消费 位置补正/坐标系统 写入的 `coord_cos/sin/originX/originY/coord_angle` 矩阵，把"模板/基准坐标系"定义的 ROI 中心与角度变换到当前图像坐标；工具未开 `useCorrection` 或无补正数据时零开销原样返回
- **11 个检测/几何工具接入 `useCorrection` 开关**（默认关，不破坏既有流程）：EdgeDetection/CircleDetection/BlobAnalysis/ColorDetection/WidthDetection/LineDetection/Caliper/VertexDetection/MultiContourMatch/BrightnessCheck/ThreadInspection
- **补 `PositionCorrection` 写 `coord_angle`**（与 CoordSystem 一致，此前缺角度跟随）
- **测试**：test_tools_regression 新增测试4——注入补正矩阵(原点60,40/角10°)，LineDetection 按参考坐标(300,220,20°)定义 ROI，验证线被找到且中心/角度落在补正后位置(317,309,30°)；反向对照：关闭跟随时窄 ROI 找不到补正后的线

**代码质量止血 + 数据正确性（2026-09-01，依据《代码质量与可靠性排查》）**：
- **H-9** ModbusComm INT32：`qint32<<16` 有符号左移 UB → 改 quint32 组合 + memcpy（含符号扩展错误修复）
- **H-10** SerialPort 裸指针泄漏：打开重入先删旧对象；关闭分支 close 后 delete（原只 close 不 delete，反复开关泄漏）
- **H-20** SerialPort/Ethernet use-after-free：delete 前先拷贝 errorString；Ethernet 断开分支补 delete socket（原只断开不释放，重连覆盖旧指针泄漏）；服务器模式删指针前已有拷贝无需改
- **H-21** UpdateView 每次 execute `new cv::Mat` 存裸指针无消费者 → 泄漏：改存命名图像（shared_ptr 值语义）
- **H-22** TemplateMatch：SQDIFF/SQDIFF_NORMED(0/1) 最佳匹配取 minLoc（原统一 maxLoc 坐标/分数错）；matchTemplate 包 try/catch（模板大于原图异常不再崩溃，属性对话框试运行也受益）
- **M-19** BlobAnalysis 连通性两分支相同失效：改 connectedComponentsWithStats 原生支持 4/8 连通
- **M-20** ThreadInspection 斜牙角叠加文本硬编码 0.0：新增 m_slantAngle 成员，overlays 显示真实斜牙角

**几何测量补齐（2026-09-01，功能缺口 §2.4"几何测量为最大短板"）**：
- 新增 **两线交点 LineIntersect**：输入两直线(中心+角度)，输出交点/两线夹角/平行标记；支持 `$(工具.键)` 属性引用直连上游检测直线
- 新增 **点到线距离 PointToLine**：输入点与直线，输出垂直距离/有向距离/垂足；同样支持属性引用
- 对齐 CKVision"几何对象→几何运算"链（线到线/点到线），为"两线夹角/交点到圆心"类组合测量打基础
- test_tools_regression 新增测试5：两线交点(400,240)+夹角90°+平行对照、点到线距离40+垂足；205 项检查全过
- 新增 **圆到线距离 CircleToLine**：输入圆心与直线(中心+角度)，输出垂直距离/有向距离/垂足（圆心在线上的投影）
- 新增 **圆到圆距离 CircleToCircle**：输入两圆(圆心+半径)，输出圆心距 centerDistance/边缘间隙 gap/同心度/关系判定(外离/外切/相交/内切/内含)
- 以上均支持 `$(工具.键)` 属性引用，可直连"检测圆形/检测直线"输出；test_tools_regression 新增测试8：圆到线距离40+垂足、圆到圆圆心距60+间隙-20+相交、同心度0；228 项检查全过

**M-43 死属性激活（2026-09-01，属性声明但从未生效）**：
- **ShapeMatch** angleRange/scaleRange 此前未读（无旋转搜索）→ 实现角度搜索：模板绕质心旋转采样 + **旋转敏感点距（质心+尺度归一化）**定最佳角；matchShapes 分数语义不变（阈值兼容旧流程）；输出 matchAngle/matchScale
- **GrayscaleMatch** angleStart/End/Step 与 scaleMin/Max 此前未读 → 实现旋转+缩放搜索：模板域内多姿态 matchTemplate 取全局最佳（含反旋转坐标换算与重复候选去重）；输出 matchAngle/matchScale
- **BlobAnalysis** useEllipse/useBBox 此前未读 → 输出主斑最小外接矩形（bbox 长宽+角度 0-180 规范化）与 fitEllipse 主轴椭圆；补 ≥5 点保护防崩溃
- **VertexDetection** detectPosition/filterHalfWidth/scanWidth/polarity 此前全死 → 改为 |dx·dy| 角点响应（角点处水平+垂直梯度同时显著）+ 平滑滤波 + 扫描步长 + 极性过滤 + 位置策略（起始/最近/最远/最强）
- test_tools_regression 新增测试9：Blob 外接框/椭圆特征、顶点"最远"策略、ShapeMatch L 形旋转搜索（量值≈±30°）；236 项检查全过
- **Calibration** applyTo 此前声明未读 → 实现：应用范围=所有流程时除写 context 外，同时写 GlobalVariables（calibration_ratio/mm_per_pixel/pixel_per_mm/unit），跨流程共享标定
- **CircleDetection / LineDetection** tolerance 此前未读 → 拟合 RMS 超差判定：rms>tolerance 则 NG+fitFailed（默认放宽到 10px 防误伤真实图）
- **MultiContourMatch** sizeTolerance 此前未读 → 面积一致性过滤：与中位数面积偏差>容差%的轮廓剔除（输出 medianArea/sizeFiltered）
- **UpdateView** imageSource 此前未读 → 0=当前图像(Current) / 1=输入图像(getInputImage)
- test_tools_regression 新增测试9d/9e/9f：applyTo 写全局变量、多轮廓面积过滤(3/4保留)、圆拟合超差NG；244 项检查全过
- 仍待处理死属性（占位工具或需库支撑）：BarcodeReader barcodeType/useROI/roiX/roiY/roiW/roiH（需条码解码库）、OCR charType（需Tesseract识别后过滤）、Ethernet protocol（当前仅TCP，选项集天然为单值）、YOLOv8Detect modelPath 等（经属性对话框/其它路径读取，待甄别）

**逻辑/通讯正确性批修（2026-09-01，M 系列实锤）**：
- **M-24** ConditionBranch `==`/`!=` 判 0：qFuzzyCompare(a,0) 恒 false → 改 `fabs(a-b)<1e-9` 容差比较（"==0"判断此前必错）
- **M-25** SerialPort 枚举索引越界：两处实现（comm/SerialPort + tools/SerialPort）JSON 加载的索引未校验 → `qBound` 保护 portName/baudRate/dataBits/stopBits/parity 全部索引
- **M-39** Modbus parity 恒 NoParity：ModbusRead/ModbusWrite/PlcLink 传枚举索引字符串给 ModbusRtuMaster → 按索引映射为"无/偶/奇"
- 顺带修复 tools/SerialPort（重复实现）：同 H-20 use-after-free（delete 后 errorString）+ H-10 关闭不释放泄漏

**H-7 密码明文落盘修复（2026-09-01，安全短板）**：
- UserManager 此前将密码**明文**存 config/users.json（M-1 用户登录遗留）→ 改为 SHA-256 双次哈希 + 每角色随机盐，存储格式 `sha256$盐$哈希`
- 旧版明文 users.json 加载时自动识别并原地升级为哈希后回写（明文不落盘）
- 空密码（初始状态直接登录）语义保持：不落哈希、空输入直接登录
- 新增 tests/test_usermanager（17 项）：哈希存储无明文、正确/错误密码登录、重启持久化、旧明文升级、空密码逻辑；ctest 13/13 全过

**H-4/H-5 工具状态线程安全（2026-09-01，并发数据竞争实锤）**：
- **H-4** PropertyResolveGuard 在工作线程改写工具 m_properties 与 UI 属性编辑竞态 → ITool 基类新增 `mutable std::mutex m_stateMutex`，propertyValue/setProperty/properties 全加锁
- **H-5** UI 线程枚举 resultData().keys() 与工作线程 setResultData 写竞态 → resultData()/setResultData/judgments/setJudgments/hasJudgments/evaluateJudgments 全加锁
- **M-48 兜底**：properties()/resultData()/judgments() 由返回引用改为**锁内快照副本**（杜绝 UI 线程读半写状态）；setStatus/status 加锁；toJson/fromJson 序列化段加锁
- **回归**：test_engine 新增测试6——工作线程高频写属性/结果/状态 vs 主线程高频读，400ms 压力下无崩溃/无半写 QVariant；全套 13 项 ctest 通过
- **H-4b 覆写派生类锁贯通（2026-09-01）**：YOLOv8Detect 此前覆写 setProperty/propertyValue 直写 m_properties 与自身成员，绕过基类 m_stateMutex（H-4 加固遗留缺口）→ ITool 增 protected `withStateLock`（锁内执行回调）/`propertyValueLocked`/`setPropertyLocked`，YOLOv8Detect 覆写改走锁（锁内直读属性+propertyDefs 默认值，避免二次加锁死锁）；test_ai 模型加载/推理/共享 Session 全通过

**色彩空间正确性（2026-09-01，M-40/M-41）**：
- **M-40** ColorConvert 灰度→HSV/LAB 此前只 GRAY2BGR 停留在 BGR：先复制 3 通道再真转 HSV/LAB，输出通道数正确
- **M-41** ColorDetection colorSpace 属性读取后未使用（恒 BGR2HSV）：按 RGB/HSV/LAB 分支选择转换与通道范围

**EdgeDetection M-42（2026-09-01）**：
- "全部边缘"此前只取第一条 → 输出全部边缘点列表 edgePoints（嵌套 {x,y,grad} 三元组，修复 QList::append(QVariantList) 拍平重载陷阱）
- filterHalfWidth 此前读取未用 → 用于剖面移动平均平滑
- test_tools_regression 新增测试6：合成阶跃图验证"全部"输出 + "最强"位置选择；211 项全过

**标定真实化（2026-09-01，P0-3，功能缺口 §3.3：opencv_calib 已捆绑未用）**：
- Calibration"棋盘格标定"从简化比例换算升级为**真角点检测**：`findChessboardCornersSB` + `cornerSubPix`，由相邻角点间距(中位数抗外点)与实际格子尺寸自动计算 mm/pixel
- 新增属性：boardCols/boardRows 内角点行列、squareSize 格子边长(mm)；输出 cornerGrid 角点网格、cornerCount、avgPixelPerSquare；检测失败返回 NG+错误信息
- Calibration 插件补链接 OpenCV（calib/imgproc/objdetect 模块）
- testdata_gen 新增棋盘格标定板生成（正交 + 旋转12° 两张，9×6 内角点、单格 60px、理论比例 10/60≈0.1667）
- test_tools_regression 新增测试7：两张棋盘格图角点数=54、比例误差<1%、上下文写入一致；220 项检查全过

**相机标定·多图内参/畸变（2026-09-01，P0-3 §3.3，opencv_calib 启用）**：
- Calibration 新增第 4 种方法 `calibMethod=3`「相机标定(多图)」：读取目录内多张不同姿态棋盘格图 → 每张 findChessboardCornersSB + cornerSubPix → 收集 objectPoints/imagePoints → `cv::calibrateCamera` 求内参矩阵 K + 畸变系数 + 重投影误差
- 新增属性 imageDir（棋盘格图片目录）；输出结果键 fx/fy/cx/cy、distCoeffs、reprojectionError、viewCount/totalViews、calibrated
- 上下文写入 `calibration_camera_fx/fy/cx/cy`、`calibration_camera_dist`、`calibration_camera_matrix`、`calibration_reprojection_error`；applyTo=所有流程 时同步写全局变量，供去畸变/3D重建下游使用
- testdata_gen 新增 calib_cam 组：用已知内参（fx=fy=800, cx=320, cy=240）+ 6 组不同旋转/平移姿态做**真实透视投影**生成（10×7 方格对应 9×6 内角点，单格 10mm，相机 z≈270mm 保证单格≥25px）
- test_tools_regression 新增测试10：6 姿态全部检出 54 角点、viewCount≥4、恢复 fx/fy≈800（容差10%）、cx/cy≈320/240（容差30px）、重投影误差<0.5px、上下文一致；252 项检查全过

**坐标校准·多点仿射/透视（2026-09-01，对标 CKVision 坐标校准1/2）**：
- 新增 `CoordinateCalibration` 工具（标定校准类）：≥3点(仿射)/≥4点(透视) 图像坐标↔目标(机械)坐标映射
- `estimateAffine2D`(RANSAC) / `findHomography` 拟合变换矩阵；输出 rms/maxErr/usedPoints 残差评估标定质量；queryX/Y 查询点变换输出 resultX/Y
- 属性 srcPoints/dstPoints 用 "x1,y1;x2,y2;..." 点集编码；上下文写 calibration_transform_*(矩阵分量+rms)；applyTo=所有流程 同步写全局变量
- test_tools_regression 新增测试11：已知仿射(30°+1.2倍+平移)/透视单应反标定，rms<0.01px、查询点变换一致、点数不足NG；264 项检查全过

**手眼标定·相机-机器人坐标关系（2026-09-01，对标 CKVision 手眼标定，§3.3 补齐）**：
- 新增 `HandEyeCalibration` 工具（标定校准类）：`cv::calibrateHandEye` 求解经典 AX=XB 手眼方程（相机→末端）
- 输入 rBase/tBase（基座→末端 罗德里格斯/平移组）+ rCam/tCam（标定板→相机）；≥3 组有效位姿；算法可选 Tsai/Park/Horaud/Andreff
- 残差评估用**闭环一致性 A·X·B=C**：各组链式矩阵经四元数中值旋转+中值平移作基准，输出旋转/平移残差 rms 与 maxErr（不受 C 绝对位姿影响）
- 查询点 queryX/Y/Z 相机系→末端→基座系 级联变换；上下文写 handeye_*；applyTo=所有流程 同步写全局变量
- test_tools_regression 新增测试12：物理一致合成（真值 X 30°+平移(10,-20,50) + 固定 C + 8 组大旋转机器人位姿，按 A·X·B=C 反推 B）反标定，Tsai 恢复 rvec≈(0,0,0.5236)/tvec≈(10,-20,50)，残差<0.5°、位姿不足NG；273 项检查全过、工具注册 56 个

**系统时间·时间戳/耗时计时（2026-09-01，对标 CKVision 计算时间/系统时间，§2.7 补齐）**：
- 新增 `SystemTime` 工具（逻辑类）：模式0 时间戳输出当前系统时间（datetime 字符串 + unixMs）；模式1 耗时计时输出自首帧起的 elapsedMs/lastIntervalMs/runCount（测单流程周期、节拍等待）
- test_tools_regression 新增测试13：时间戳 unixMs>1.7e12 合理、耗时模式 runCount 递增且 elapsedMs 单调非减；282 项检查全过、工具注册 57 个

**流程内全局变量读写（2026-09-01，对标 CKVision 获取/设置全局变量，§2.7 补齐）**：
- 新增 `GetVariable` 工具（逻辑类）：从全局变量管理器读取变量（跨流程共享、线程安全），输出 value/typeName/found；与 设置变量"写入全局"或标定 applyTo=所有流程 写入的全局结果配合
- `SetVariable` 增强 scope 属性（当前流程/全局变量）：scope=1 写入 GlobalVariables（此前仅写 context），默认 scope=0 保持原行为
- test_tools_regression 新增测试14：SetVariable 全局写入→GetVariable 读回一致、不存在 found=false、空变量名 NG、scope=0 不污染全局；294 项检查全过、工具注册 58 个

**图像去畸变工具（2026-09-01，对标 CKVision 去畸变，§3.3 标定链闭环）**：
- 新增 `ImageUndistort` 工具（图像处理类）：用相机内参+畸变系数去畸变；参数来源两种——相机标定（Calibration 方法3）写入上下文的 calibration_camera_*，或手动输入 fx/fy/cx/cy + k1/k2/p1/p2/k3
- 采用 initUndistortRectifyMap + remap；输出去畸变图像写入当前图像流供后续检测/测量使用；结果键 undistorted/source/fx/fy/cx/cy/reprojError（标定路径透传重投影误差）
- test_tools_regression 新增测试15：解析前向畸变模型合成畸变点阵（k1=0.4 角点显著位移）→ 手动内参路径去畸变点阵恢复、标定上下文路径去畸变+reprojError 透传、无输入 NG；305 项检查全过、工具注册 59 个

**文本三件套（2026-09-01，对标 CKVision 生成/分解/比较文本，§2.7 补齐）**：
- 新增 `GenerateText`（生成文本）：模板 {1}..{5} 直接变量 + {key} 上下文/全局变量替换，用于拼装结果报文/标签/日志，输出 text/replacedCount
- 新增 `SplitText`（分解文本）：按逗号/分号/空格/竖线/自定义分隔符拆段，输出 partCount/partN/selected，用于解析通信报文
- 新增 `CompareText`（比较文本）：等于/不等于/包含/开头/结尾/正则匹配，可区分大小写，不匹配状态置 NG
- test_tools_regression 新增测试16：模板替换/缺失键保留/分解取段/自定义分隔符/区分大小写/包含/正则匹配/正则不匹配；329 项检查全过、工具注册 62 个

**线到线测量工具（2026-09-01，对标 CKVision 线到线，§2.4 补齐）**：
- 新增 `LineToLine`（线到线）：两线间距（线1中心到线2垂直距离，平行线即精确间距）+ 两线夹角（锐角折叠[0,90]）+ 平行判定，输出 distance/angleBetween/parallel/found
- 输入沿用"直线中心+角度"表示法，支持引用上游检测直线输出（$(工具名.键)）
- test_tools_regression 新增测试17：平行水平线间距50、垂直相交线线1中心到竖直线2距离200/夹角90、45°斜线垂距10/√2/夹角45；340 项检查全过、工具注册 63 个

**延伸点/旋转点几何点运算（2026-09-01，对标 CKVision 延伸点/旋转点，§2.4 补齐）**：
- 新增 `ExtendPoint`（延伸点）：起点沿方向角延伸指定长度计算终点（endX/endY/deltaX/deltaY），用于直线端点外延取辅助参考点
- 新增 `RotatePoint`（旋转点）：点绕旋转中心旋转指定角度计算新坐标（resultX/resultY），用于工件旋转后推算特征点理论位置
- 两工具输入均支持引用上游输出（$(工具名.键)）
- test_tools_regression 新增测试18：延伸点90°→(100,150)/水平→(125,100)、旋转点(10,0)绕原点90°→(0,10)/中心点不动；352 项检查全过、工具注册 65 个

**数据队列工具（2026-09-01，对标 CKVision 队列，P0-9 补齐）**：
- 新增 `DataQueue`（数据队列）：流程内命名队列（FIFO），跨工具/跨执行持久保存，用于缓存连续N帧检测数据供统计/判定
- 操作：入队（值/上下文/全局变量取源）、出队（空返回默认值）、窥视、清空、长度；存储全局变量命名空间 `__DataQueue.<队列名>`（QVariantList），静态互斥锁线程安全
- test_tools_regression 新增测试19：空窥视默认值、入队3→长度3、窥视不移除、FIFO出队、跨执行持久、清空后出队默认值、空队列名 NG；376 项检查全过、工具注册 66 个

**循环控制闭环（2026-09-02，对标 CKVision 循环/停止循环，P0-9 灵魂 + M-23 修复）**：
- 引擎 `FlowEngine::doExecute` 由 for 改为带执行索引的 while，**消费工具写入的 context.message()**（此前 Loop 的 setMessage(1) 无消费方，循环是断头的）：
  - message==1 循环回跳 → 跳到 `__loop_start`；message==2 停止循环 → 跳到 `__loop_end` 之后；message==3 无条件跳转（预留）
  - 每轮重置 `__loop_active/__loop_break/__flow_ng` 防残留；200000 步死循环保护
- 新增 `LoopEnd`（循环结束）与 `StopLoop`（停止循环），与重写后的 `Loop`（开始循环）组成结构化循环：Loop 声明起点/索引/模式，LoopEnd 判定是否回跳，StopLoop 按条件（无条件/流程NG/数据满足）立即退出
- `Loop` 重写：循环模式 递增A→B-1 / 递减A-1→B / 无限，索引写入 `indexKey`（默认 loopIndex），循环次数 B-A
- test_engine 新增测试7/8/9（引擎级）：有限循环体执行3次、StopLoop(loopIndex>=3)跳过后续、无条件停止兜底路径；ctest 13/13 全过

**状态/文本/声音通讯四件套（2026-09-02，对标 CKVision 播放声音/写入文本/文件监测/收发数据，P0-10 补齐）**：
- 新增 `PlaySound`（播放声音）：NG 报警音——系统提示音或异步播放 .wav（Windows PlaySoundW，无第三方依赖；类名与 windows.h 宏冲突已 #undef）
- 新增 `WriteText`（写入文本）：目录/文件名/日期与时分秒后缀/扩展名/追加或覆盖，内容支持 `$(工具名.键)` 引用（引擎自动解析）
- 新增 `FileWatch`（文件监测）：检查信号文件存在/删除文件/清理超保留天数旧文件——现场低成本握手（上位机落文件对接）
- 新增 `TcpData`（TCP收发）：单次原子 连接→发送→接收→断开，适合 NG 上报/一次性握手（与持久连接的"以太网"工具互补）
- test_tools_regression 新增测试20：播放声音执行路径、写入文本追加/覆盖/内容验证、文件监测出现/删除/清理、TCP 本地回环 echo（发送并接收/拒连 NG）；403 项检查全过、工具注册 72 个

**斑点分类 BLOB 特征分类（2026-09-02，对标 CKVision 斑点分类，P1-13 补齐）**：
- 新增 `BlobClassify`（斑点分类）：对二值化连通域逐斑提取 面积/周长/圆度/长宽比/孔数 特征，支持面积/圆度/长宽比/孔数 阈值过滤 + 有效数 [minCount,maxCount] 判定
- 实现要点：RETR_EXTERNAL 取外边界轮廓（避开 OpenCV 5 RETR_CCOMP 层级字段错乱不可靠的问题）；孔数用"bbox 内反转 + 不与 bbox 边界接触的背景连通域"统计，不依赖轮廓层级；圆度用拟合椭圆长短轴比（对像素栅格化稳健，圆≈1）
- test_tools_regression 新增测试21（合成图：圆/矩形/圆环）：混合图3斑、圆度>0.9只留圆+环、长宽比>2只留矩形(≈3)、孔数>=1只留圆环、面积过滤、minCount 判定 NG/OK；421 项检查全过、工具注册 73 个

**图像处理补充（2026-09-02，对标 CKVision 图像运算/存储图像，P1-11 部分补齐）**：
- 新增 `ImageOperation`（图像运算）：两图像 9 种逐像素运算（加/减/差分/与/或/异或/最小/最大/平均），输入1 用 inputImage 属性、输入2 用 inputImage2 命名槽属性（引用任意工具输出图），支持 ROI 裁剪后运算、尺寸/类型自动对齐，输出写 Current 供后续引用
- 新增 `SaveImage`（存储图像）：把当前/指定命名图像存盘，目录自动创建，文件名可加日期/时分秒后缀，PNG/JPG/BMP 三格式（JPG 可设质量），输出 filePath/saved
- 复用引擎"工具输出图按实例名入命名槽"机制（FlowEngine 已有），实现任意两工具输出图参与运算
- test_tools_regression 新增测试22：加饱和255/减140/差分/平均130/与/异或/输出尺寸 + PNG 保存回读（像素/尺寸一致）+ JPG 路径；443 项检查全过、工具注册 75 个、ctest 13/13
- 待后续：图像合并（拼接/通道合并）、裁剪变换（镜像/旋转/缩放/平移/仿射）、切换图像、隔行采样

**图像处理补充·合并与裁剪（2026-09-02，P1-11 收尾）**：
- 新增 `ImageMerge`（图像合并）：最多4张图按 水平/垂直/网格 合并成一张大图，支持水平/垂直重叠量（重叠区取两图平均值混合去接缝），尺寸自动缩放对齐
- 新增 `CropTransform`（裁剪变换）：ROI 裁剪 + 镜像（水平/垂直/双向）+ 旋转（90/180/270/任意角度，以中心为原点）+ 缩放（比例）+ 平移（X/Y，黑色填充），输出写 Current 供后续引用
- 关键可靠性修复：工具内输入图像统一 `clone()` 后再就地变换——浅拷贝共享缓冲会导致 flip/rotate 污染源图（回归测试曾因源图被污染而 3 项失败）；同时补设 `m_roi.type` 使 ROI 裁剪正确生效
- test_tools_regression 新增测试23：水平/垂直/网格/重叠合并尺寸+像素；镜像/旋转180/90/缩放2x/平移/ROI 裁剪位置与尺寸断言；476 项检查全过、工具注册 77 个、ctest 13/13
- P1-11 四项（图像运算/合并/裁剪变换/存储图像）全部闭环；待后续：切换图像、隔行采样

**像素统计 + 检测间距 + 扫描边缘（2026-09-02，P1-12）**：
- 新增 `PixelStatistics`（像素统计）：ROI 裁剪 → 灰度基础统计（mean/stddev/min/max）+ 阈值区间 [low, high] 白像素数与比率 + 上下限判定 OK/NG
- 新增 `EdgeSpacing`（检测间距）：卡尺式扫描线沿角度检测全部边缘 → 相邻边缘间距计算（宽度/中心位置），支持起始/最后/最宽/全部四种模式，宽度上下限判定
- 新增 `ScanEdge`（扫描边缘）：多条并行扫描线检测全部边缘点坐标（按扫描方向排序），支持起始/最后/最强三种模式，输出主边缘坐标与梯度
- 复用 SubpixEdge 亚像素边缘基础设施，边缘去重（相邻 <1px 合并避免重复峰），精度与 Caliper 同级
- test_tools_regression 新增测试24：像素统计全图/ROI 白像素+均值+极值；检测间距单条/最宽模式；扫描边缘起始/最强模式；494 项全过、工具注册 80 个、ctest 13/13
- P1-12 三项全部闭环

**全工具可用性打磨·通用交互批次（2026-09-05）**：
- **工具箱搜索框**：按显示名/类型名实时过滤、空分类自动隐藏（80+工具翻树难找）；悬浮提示补 名称/分类/类型名
- **属性对话框即改即显**：参数改动 300ms 防抖自动试执行预览（原只能手动点按钮）；`PropertyDef.tooltip` 声明此后真正渲染（标签+编辑器悬浮可见）；数据判定表在试执行拿到结果键后自动补建（原必须先执行整条流程）
- **复制粘贴携带完整参数**：剪贴板由"仅类型名"改为 `ITool::toJson()` 全量（参数/判定/注释），粘贴 `fromJson` 还原；副本沿用原名、重名自动 `_N`
- **状态栏像素坐标读数**：`ImageViewWidget::cursorImagePos` 信号（此前存在但从未接线）接入全部图像格
- **GigE 连接修复**：GVCP READREG_ACK 寄存器值在偏移 12（原读 8 恒为 0）、WRITEREG_ACK 标准 12 字节（原 ≥16 校验恒失败）；补 `QThread` 头
- **82 工具中文描述库**：`ToolMetaData.description` + ToolRegistry 内置说明表（逐一对照插件 execute() 核实，条码识别如实标注占位）；工具箱悬浮/属性对话框首页/搜索关键词三处展示（搜"报警"能找到播放声音）
- **ROI 图形化拖拽编辑**：属性对话框试执行页新增"在图上拖拽编辑ROI"——预览图上画青色ROI框（8手柄+中心十字），拖框移动、拖手柄沿旋转轴对称缩放，数值实时回写编辑器并自动重跑预览；支持带角度ROI（按 u/v 轴投影命中与缩放）；适用 roiCenterX/Y+roiWidth/Height 约定的 34 个工具，ROI类型"无"时启用自动切"矩形"；ImageViewWidget 拖拽中忽略外部 setRoiRect 防止刷新打架
- **流程撤销/重做（Ctrl+Z/Ctrl+Y）**：工程级快照栈——ProjectManager 新增 currentStateJson/restoreStateJson（与 loadProject 共用同一重建路径，先停引擎再重建），MainWindow 在全部 7 类变更点（添加/删除/粘贴/上下移/重命名/启停/属性确定）后压栈，上限 50 步；运行中禁止撤销；新建/打开/模板后重置基线；project_roundtrip 回归验证重构无损
- **条码识别真实解码落地**：发现 OpenCV 5.0 已把一维条码检测并入 objdetect 模块且捆绑 DLL 已含符号——零新依赖直接接入 cv::barcode::BarcodeDetector；输出 barcodeText/barcodeType/barcodeCount/boxQuads/centerX/Y，叠加层画条码四角绿框；删除恒不生效的 barcodeType 死属性；回归新增 5 项检查（无码优雅NG/结果键/ROI路径），总计 499 项零硬失败
- **ROI 拖拽补齐角点约定**：PropertyDialog 同时识别 中心约定(roiCenterX/Y+Width/Height) 与 角点约定(useROI+roiX/Y/W/H，条码/二维码/OCR)；启用拖拽时自动勾选 useROI；整数 QSpinBox 参数读写兼容
- 回归：ctest 12/13（pkg_infer 为既有环境缺部署模型文件，非本次引入）；离屏启动冒烟通过

## 阶段6：平台化与商业交付

设备模板系统（视觉筛选 / 视觉贴标 / 视觉计数 / 木业检测模板）、DIY 界面编辑器完善、数据记录与报表、安装包与加密授权、远程日志与升级。

**设备模板系统①（2026-09-05 完成）**：
- **用户自定义模板**：文件菜单新增 [保存为模板...] / [从模板新建...]——把调好的工程一键存入 `templates/` 目录（exe旁，部署包随包携带），ProjectManager 新增 saveTemplate/loadTemplate（loadTemplate 与加载共用 restoreStateJson 重建路径，但清空当前项目路径 = "未保存的新工程"语义）
- **模板库画廊**：TemplateDialog 列出 内置筛选机模板 + 用户模板（显示说明/流程数/工具数/保存时间），双击即建
- 通用工具异常属性名容错保持；从模板新建后撤销栈重置
- 待续：贴标/计数/木业更多内置模板、模板缩略图

**数据记录与报表①（2026-09-05 完成）**：
- **检测记录 SQLite 持久化**：`DetectionRecorder`（VIEngine，可选依赖 Qt6::Sql，无 Sql 时空实现自动降级）——每次执行结果落库 exe旁 `data/records.db`（时间戳/总结果/项目判定JSON/数值JSON），关机不丢；支持 时间段+OK/NG 过滤查询、按过滤导出CSV(UTF-8 BOM)、按保留天数清理
- **历史记录查询对话框**：数据菜单 [历史记录查询...]——日期范围+结果过滤、明细表格、导出CSV、清理旧记录（二次确认）
- **Qt SQLite 驱动部署修复**：驱动插件必须位于 `<exeDir>/sqldrivers/` 子目录（此前 bin 里平铺的 qsql*.dll 是无效的）——CMake 构建后自动从 Qt6::QSQLiteDriverPlugin 导入目标拷贝（主程序+测试）
- 回归：新增 test_detection_recorder 19 项（写入/倒序查询/OK-NG过滤/时间过滤/CSV含BOM与数值/空库清理/关闭后安全），ctest 15/16 通过（pkg_infer 仍为既有环境问题）

**设备模板系统②·四类内置模板（2026-09-05 完成）**：
- **DeviceTemplates 模块**（VICore）：筛选机/视觉贴标/视觉计数/木业检测 四类模板的 定义+构建 集中一处（画廊/主窗口/回归测试共用），新增内置模板只需 all() 加一条
- 画廊内置条目改为循环生成（builtin:<id>），主窗口 onNewFromTemplate / onOpenTemplateGallery 改走 DeviceTemplates::build（原硬编码 18 工序序列迁入模块，行为不变）
- **deploy.bat 打包链路补齐**：显式拷贝 sqldrivers/qsqlite.dll（windeployqt 之外的双保险）+ 创建 templates/ data/ 目录占位；顺带修复上批误写入的控制字符
- 回归：新增 test_device_templates 26 项（4模板×工具注册齐全/构建成功/工具数一致/实例名无重复/byId反查 + 未知id失败），ctest 16/17 通过（pkg_infer 既有环境问题）

**DIY 运行界面闭环（2026-09-05 完成）**：
- **RuntimeUI 运行时渲染器**（核心缺口修复）：此前 UIEditor 编辑的布局从未被运行时加载（纯孤岛）——新增 RuntimeUI 把布局 JSON 渲染成真实操作员界面：ImageView=实时检测图查看器、DataTable=工具结果表、ValueDisplay=绑定键数值大字显示（"工具名.结果键"随执行刷新）、Button=操作按钮（绑定 run/start/stop 动作）、StatusPanel=8工位状态灯（复用已有控件）
- **编辑器补绑定属性**：WidgetProxyItem 新增 bind 字段并随 JSON 序列化；属性面板加"绑定/动作"行（按类型给占位提示）；编辑器存/读对话框默认指向 config/runtime_ui.json
- **主窗口视图切换**：视图菜单 [运行界面(DIY布局)] 可勾选切换——QStackedWidget 包裹标准界面(随时切回)，切换时加载布局并回喂最近一次结果，执行结果实时同步(图像/表/数值/工位灯)，按钮动作直连 F5/F6/F7
- ctest 16/17 + 离屏冒烟通过

**安装包与授权①·软授权落地（2026-09-05 完成）**：
- **LicenseManager**（VICore，Windows CNG/bcrypt.dll 零新依赖）：机器码=CPU品牌串+系统卷序列号+首网卡MAC 的 SHA256（XXXX-XXXX-XXXX-XXXX，同机稳定）；授权文件=JSON载荷{customer/expiry/machine/modules{ai,maxCameras}}+RSA2048-PKCS1签名；无授权=首运行起30天全功能试用，过期不断产→红色水印+状态栏提示+AI模块停用
- **license_gen 签发工具**（供应商侧，不进发布包）：keygen 生成密钥对（公钥头写产品源码，**私钥 D:\LWVision-keys\ 永不入仓库，.gitignore 已挡**）/ show 机器码 / sign 签发（支持模块参数）
- **关于对话框升级**：显示授权状态+机器码，[导入授权文件...] 按钮验签后落 config/license.lic 即时生效；AI工具(AIDetection/YOLOv8Detect)执行入口加模块门禁
- **CNG 踩坑记录**：RSAPRIVATEBLOB 是无CRT半钥→SignHash 报 0xC000000D，必须用 RSAFULLPRIVATEBLOB；全部环节统一 BCRYPT_RSA_SIGN_ALGORITHM 句柄（lic_probe.cpp 探针留存备查）
- 回归：test_license 11项（密钥对/最小往返/篡改载荷验签失败/异构载荷/公钥不匹配/无效导入报错），ctest 17/18（pkg_infer 既有环境问题）

**AI 检测打磨·商用级判定（2026-09-05 完成）**：
- **YOLOv8Detect 商用判定参数**：`targetClasses` 目标类别过滤（逗号分，空=全部——如忽略"良品"类只判缺陷类，计数与判定均按过滤后）；`minCount/maxCount` 数量区间判定（**漏检/多检都NG**——对标商用"必须恰有N个目标"，0=不限保持旧行为）；`inferenceMs` 单帧耗时输出（预处理+推理+后处理）；逐目标平铺键 `defN_class/confidence/x/y/w/h`（供数据判定页/显示控件逐个引用，最多50）
- **AIDetection 同步**：同款 targetClasses/minCount/maxCount 判定（检出即NG与数量区间可组合：缺陷检测=目标数清零才OK；目标存在=区间内才OK）+ inferenceMs
- 输出新键：count(过滤后)/rawCount(过滤前)/countOk/targetCount
- ctest 17/18 全过（test_ai 模型加载/推理不受影响）

---

## 横切原则（每个阶段都遵守）

- **每步可运行**：每个阶段结束程序必须能编译运行，能演示
- **测试资产**：建立标准测试图片库（OK/NG 样本），每阶段跑回归
- **git 规范**：小步提交，功能分支，定期 push 远端
- **文档同步**：每阶段更新对应文档，不留"三个盘符三个故事"的历史问题
- **范围控制**：一次只推一个阶段，做完验收再进下一阶段

## 风险与对策

| 风险 | 对策 |
|------|------|
| 移动硬盘是唯一副本 | 尽快建远端私有仓库（Gitee/GitHub） |
| 无硬件开发，真机适配可能返工 | 虚拟相机/模拟PLC先行，驱动接口保持 ICameraDriver/IPLCDriver 抽象不变 |
| 范围大（平台+AI+专项），易烂尾 | 严格按阶段推进，每阶段有明确验收标准 |
| AI 训练依赖 Python 环境 | 训练侧独立打包（训练机可选），部署机只带 ONNX Runtime 推理 |
