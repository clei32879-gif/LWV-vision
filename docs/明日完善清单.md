# LW Vision 明日完善清单（2026-09-01 工作计划）

> 依据 2026-08-31 三份子代理排查报告（`docs/排查/`）整合，按"先接线补断链、再补核心能力、后清死代码"排序。
> 原则：不引入新依赖（仅 Qt/OpenCV/nlohmann），每块改动编译通过 + 本地 git 提交；推送待用户指示。

## A. 今日已完成的补全（2026-08-31）

| 项 | 内容 | 状态 |
|----|------|------|
| A1 | `ModbusPLCDriver`（IPLCDriver 完整实现）：TCP/RTU 双通道 + 业务级吹气/伺服/契约快捷方法，接线 `HardwareManager::connectPLC` | ✅ 编译+联测通过 |
| A2 | `IPLCDriver.cpp` 便捷方法默认实现（readRegister/writeInt32/readFloat 等） | ✅ |
| A3 | `ModbusIoDriver` 接线 `HardwareManager::connectIO`（替代 N1616 IO 卡经 Modbus 读写） | ✅ |
| A4 | **修复 TCP 从站 FC15 写多线圈位偏移 off-by-one**（pdu[5]→pdu[6]；单线圈写 false 曾被写成 true） | ✅ 全 ctest 12/12 |
| A5 | test_modbus 新增 TCP测试8（PLC 驱动业务契约全链路联测） | ✅ |
| A6 | **修复 AIDetection detectionIsNG 属性未定义**（检出即NG 判定恒 false → NG 品漏判，生产安全级，代码质量报告 H-8） | ✅ 本次顺手修复 |
| A7 | 三份子代理排查报告 + 本清单 | ✅ `docs/排查/` |

## B. 明天优先（P0 — 打通"死功能"，纯接线/低风险，收益最大）

来源：UI 报告 P0 + 功能缺口报告 P0-1 + 代码质量报告"先止血"项。

1. **UI 信号断链全部接线**（1~2 天见效）：
   - 流程编辑器右键 7 项（重命名/上移/下移/启停/复制/粘贴/删除）信号 → MainWindow 接线（含 clipboard 状态赋值）
   - 双击节点编辑属性（toolDoubleClicked 未连接）
   - Delete 键删除节点（deleteRequested 未连接）
   - 底部 CCD 检测项目表（DataPanel）真实更新（setStats/updateResult/setStationStatus + m_summaryLabel 补 new）
   - 主窗口缩放槽接到 m_multiView 并补 UI 入口；状态栏补"用户/连接"标签
2. **快捷键与编辑菜单**：死代码 MenuBar/MainToolBar 收敛——在 MainWindow 手写菜单补 Ctrl+S 保存、F5 运行、编辑菜单与撤销重做（QUndoStack）
3. **位置补正数据流打通（P0-1 核心能力）**：引擎加 CoordinateFrame 服务，PositionCorrection 写矩阵、各 Detection/Geometry 工具按补正矩阵变换 ROI/坐标、EndCorrection 清空——找圆→补正→检测组在补正坐标系内联动（流程灵魂）
4. **数据/状态反馈**：图像区像素坐标+缩放%显示（cursorImagePos 接线）；运行状态颜色化；日志默认展开；closeEvent 未保存提示
5. **线程生命周期止血**（代码质量 H-1/H-2/H-3/H-11，本项目最可能的崩溃源）：
   - 关窗/加载项目前先停流程线程并等待结束，再释放硬件与 Modbus 连接池（H-2/H-11）
   - 统一执行互斥（单次与连续运行不可并发 doExecute 同一 Flow，H-3）
   - Flow 统一管理工具所有权，删除前停引擎（H-1）
6. **生产安全项**：用户密码改 SHA256+盐、默认非 Admin（H-7）——建议明天一并做

## C. 明天次优先（P1 — 标定真实化 + 客户高频刚需）

5. **相机畸变标定 + 去畸变工具**：用已捆绑 opencv2/calib（findChessboardCorners→calibrateCamera→undistort）
6. **测量标定升级多点仿射/透视**：estimateAffine2D 多点标定 + 多方案切换
7. **手眼标定**：OpenCV calibrateHandEye + 九点标定向导
8. **OCR 转正**：自研"字符集合学习 + 分割 + 归一化模板匹配"（固定印刷体），替换"数轮廓"占位
9. **几何计算工具组**：点到线/线线夹角/交点/延伸点/圆到圆/拟合（向量运算，无新依赖）
10. **数据记录 + 统计报表**：趋势图/直方图/NG 分布/良率报表（QPainter 自绘或 Qt Charts）
11. **多相机并行流程**：每相机 1 线程 + 1 流程 + 独立 ToolContext（软触发帧对齐，硬件同步留接口）

## D. 再往后（P1/P2）

12. 逻辑控制补强：Loop break、选择分支、执行流程（嵌套）、数据队列（Loop 目前 setMessage(1) 但引擎不循环——M-23）
13. 通讯补强：播放声音报警、写入文本、文件监测（QFileSystemWatcher）、TCP 裸收发
14. 图像处理补充：图像运算/合并/裁剪输出/存储图像/像素统计/检测间距/扫描边缘
15. 斑点分类（BLOB 特征分类）、流程内设置/获取全局变量工具、数据记录节点
16. 工程自动备份 + 单实例互斥（.bak 轮换 + QLockFile）
17. 一致性清理：术语统一、图标统一（IconHelper）、假选项移除（语言切换）、UIEditor 撤销/重做、DisplayArea/空壳 widget 收敛
18. **数据正确性修复**（代码质量报告 H-9/M-19/M-20/M-24 等）：
    - ModbusComm INT32 有符号左移 UB（H-9）
    - BlobAnalysis 连通性参数失效（(conn==4)?SIMPLE:SIMPLE 两分支相同，M-19）
    - ThreadInspection 斜牙角叠加显示硬编码 0.0（M-20）
    - ConditionBranch qFuzzyCompare(0) 恒 false（M-24）
19. **并发/资源加固**（代码质量中优先级）：ModbusTcpSlave 缓冲 static 共享改成员+断开清理（M-6）、RTU 从站 0x10/0x0F byteCount 校验对齐（M-7）、VirtualCamera 超时帧语义（M-9）、DeshengCamera 抓帧移线程（M-10）、ProjectManager 写失败检测+原子替换（M-12）、ConfigManager 批量落盘/cameraCount 上限（M-13/14）、Logger 缓冲 flush+atomic（M-15）、用户密码哈希（若 B-6 未做）、工具属性/结果加锁（H-4/H-5）、Flow 所有权统一（H-6）、SerialPort/ModbusComm 裸指针泄漏+索引越界（H-10/M-25）、OCR ROI 越界 clamp（M-22）、DataJudge 原子计数（M-26）

## E. 明确不做（范围控制）

- 3D 测量（无 3D 相机规划）；焊缝/涂胶/字符验证等细分专用工具；DataMatrix（需 ZXing 新依赖，客户确需再议）。

---
*生成：2026-08-31。三份详细报告见 `docs/排查/功能缺口与工具缺失排查-2026-08-31.md`、`docs/排查/UI与交互体验排查-2026-08-31.md`、`docs/排查/代码质量与可靠性排查-2026-08-31.md`。*
